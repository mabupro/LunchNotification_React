import React from 'react';
import { Header } from '../components/Header';
import { Menu } from '../components/Menu';

export const User = () => {
    return (
        <div>
            <Header />
            <Menu />
        </div>
    )
}